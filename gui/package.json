{
  "name": "gui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "@testing-library/user-event": "^7.2.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.4.1"
  },
  "scripts": {
    "start": "sh  -ac 'cp .env.${REACT_APP_ENV} .env; react-scripts start'",
    "start:dev": "REACT_APP_ENV=dev npm run start",
    "build": "sh  -ac 'cp .env.${REACT_APP_ENV} .env; react-scripts build'",
    "build:prod.local": "REACT_APP_ENV=prod.local npm run build",
    "build:prod": "REACT_APP_ENV=prod npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
